import MoneyCounter from '../components/Counter/MoneyCounter';
import Controls from '../components/Counter/Controls';

const Counter = () => {
  return (
    <>
      <MoneyCounter />
      <Controls />
    </>
  );
};

export default Counter;
